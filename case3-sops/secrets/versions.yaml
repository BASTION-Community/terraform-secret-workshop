# ── value_wo_version 메타데이터 ─────────────────────────────
# value_wo는 State에 저장되지 않으므로 Terraform이 값 변경을 감지할 수 없다
# 값을 변경할 때마다 해당 키의 버전 숫자를 +1 한다
# 이 파일의 key = SSM parameter path, value = 버전 번호

"/demo/api-key-1": 1
"/demo/api-key-2": 1
"/demo/api-key-3": 1
